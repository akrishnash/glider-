# Glider – env example. Copy to .env and set values.

# TinyFish API key (required for running applications)
TINYFISH_API_KEY=sk-tinyfish-KQMrPB2vXnamyjiay7cbEgfvKJWS2SnG

# JWT secret for auth (use a long random string in production)
JWT_SECRET=glider-dev-secret-change-in-production

# Optional: database path (default: ./data/glider.db)
# DATABASE_PATH=./data/glider.db

# Optional: API port (default: 3000)
# PORT=3000
